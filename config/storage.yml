test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

amazon:
  service: S3
  access_key_id: AKIAJEUPSRZQUAHY4GRA
  secret_access_key: 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
  region: eu-central-1
  bucket: pivorakprojectx