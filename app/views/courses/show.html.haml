.main2
  .course-page{:style => "margin-bottom: 20px"}
    .edit-course-icon
      - if current_user&.role?('admin')
        = link_to edit_course_path(@course), method: :get do
          %span.fas.fa-pen-square
    .course-page-img
      - present @course do |course_presenter|
        = image_tag course_presenter.logo
    .course-page-info
      %h2
        = @course.title      
      .course-page-rating
        = rating_for @course, 'rating', readonly: true, noRatedMsg: ''
      .tag-list
        - (@course.tag_list).each do |tag|
          %a.tag{:href => "#"}
            = tag
      %p
        = @course.description
      %ul.course-info-list
        %li
          %i.fas.fa-globe-americas
          = link_to @course.url, @course.url
        %li
          %i.far.fa-building
          - if @course.organization.present?
            = link_to @course.organization.name, @course.organization.site
          - else
            = t('courses.all.none')
        %li
          %i.fas.fa-map-marker-alt
          = @course.city.name
        %li
  .course-page{:style => "margin-bottom: 20px"}
    .stylish-title.clearfix
      .hr
      .stylish-title-text
        = t('reviews.all.review-title')
      .hr
    .comments
      = render policy_scope(@course.reviews.includes(:author))
      = render 'reviews/form'
-#   .row.clearfix
-#     .col.center-block.text-center
-#       .fb-comments{"data-href" => "http://localhost:3000/courses/" + "#{@course.title}", "data-numposts" => "5", :style => "background: white"}

-# #fb-root
-# :javascript
-#   (function(d, s, id) {
-#     var js, fjs = d.getElementsByTagName(s)[0];
-#     if (d.getElementById(id)) return;
-#     js = d.createElement(s); js.id = id;
-#     js.src = 'https://connect.facebook.net/en_US/sdk.js#xfbml=1&version=v3.1&appId=1019055704927361&autoLogAppEvents=1';
-#     fjs.parentNode.insertBefore(js, fjs);
-#   }(document, 'script', 'facebook-jssdk'));
      
